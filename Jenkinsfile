pipeline {
    agent any

        stages {
            stage ('Inicial') {
                steps {
                    echo 'Iniciando a pipeline no petshop system'
                }
            }
        }
}